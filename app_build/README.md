# app_build

This directory holds the application code inputs and outputs.

- `figma_seed/` — place the Figma-generated code here
- `legacy_reference/` — optional, place old repo/code reference here if needed
- `museio_app/` — Antigravity should build the real production app here
